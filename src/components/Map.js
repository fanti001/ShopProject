import { useLoadScript, GoogleMap, MarkerF } from '@react-google-maps/api';
import { useMemo } from 'react';


const Map = () => {

   return (
      <div className="map">

      </div>
   );
}

export default Map;